# samples/ — public / educational data

Fictional or anonymized case data, safe for GitHub and the 3-day workshop. Distinct from
`clients/` (real, gitignored) and from `core/schemas/examples/` (schema-validation fixtures —
also fictional, but scoped narrowly to demonstrating the schema rather than a full case).

Empty for now. A public, anonymized case will be added here if/when one is needed — see
`README.md` at the project root, "Four content areas".
