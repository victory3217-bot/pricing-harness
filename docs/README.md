# docs/ — SPEC / METHOD / CASE per feature

Empty for now. As each feature is implemented (starting with MODE A), its
`docs/features/<feature>/SPEC.md` / `METHOD.md` / `CASE.md` trio goes here — see the project
root `README.md`, "SPEC / METHOD / CASE".
